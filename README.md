# Gbillions — NFT Card Generator

A ready-to-run React + Vite + Tailwind app that lets users upload a DP, pick roles, and export a blue/white futuristic NFT-style card as PNG.

## Quick start
```bash
npm install
npm run dev
```
Open http://localhost:5173

## Build
```bash
npm run build
npm run preview
```

## Deploy
Push to GitHub, then import the repo on Vercel.
